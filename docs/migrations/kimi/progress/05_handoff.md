# Phase 05 handoff

Kimi chat stores bounded public history separately from administrator history. An administrator storage key includes the actor ID and never reads the group/public history. Vision slots are rendered into the Kimi request envelope and are saved only after a successful response.
