# AGENTS.md

本文件用于约束 Codex / 代码代理在本仓库中的修改行为。

## 目标

这是一个已经可运行的 QQ 机器人项目。修改时优先保证：

1. **不破坏现有功能**
2. **只做最小必要修改**
3. **不要引入编码、换行符、路径、安全边界问题**
4. **不要为了“更整洁”而大规模重构**

如果需求可以通过小范围补丁完成，**禁止**改写整文件。

---

## 硬性规则

### 1) 最小修改原则

- 只修改完成当前任务所必需的文件和代码。
- **不要**顺手修复无关问题。
- **不要**做大规模重命名、搬移、拆文件、批量格式化。
- **不要**仅因为“代码风格更统一”而改动现有实现。
- 除非任务明确要求，**不要**改动 public 接口、命令字、配置键名、持久化数据格式。

### 2) 编码与换行符规则

- 所有文本文件统一使用 **UTF-8** 编码。
- 优先保持文件当前换行风格；如果任务明确涉及修复换行问题，只允许对**当前目标文件**处理。
- **不要**在一次任务中批量改动全仓库的换行符。
- **不要**因为编辑器自动保存导致整文件编码/换行发生变化。
- 遇到含中文注释或中文提示文本的文件，禁止整文件重写。

### 3) 高风险文件规则

以下文件或同等级大文件属于高风险文件，修改时必须格外保守：

- `commands.py`
- `aisvc.py`
- `blackboard_ocr.py`
- `handinsvc.py`
- `client.py`
- `onebot.py`

规则：

- 仅允许局部补丁式修改。
- **不要**重排 import、**不要**大面积整理注释、**不要**大段重构。
- 如果改动这些文件，优先控制在与需求直接相关的最小范围内。

### 4) 安全与权限规则

- **不要**弱化路径安全检查。
- **不要**移除 `safe join`、root 约束、文件范围限制等安全边界。
- **不要**默认放宽权限、自动审批、访问范围、日志暴露范围，除非任务明确要求。
- 涉及文件发送、压缩、目录扫描时，不得突破既有访问控制逻辑。

### 5) 异步与性能规则

- 这是异步机器人项目。
- 新增明显重 I/O 或重 CPU 的逻辑时，优先考虑不要阻塞事件循环。
- **不要**在消息主路径中随意加入长时间同步扫描、同步压缩、同步大文件复制。
- 如果只是小修，优先保持现有架构，不做无必要的并发模型重写。

---

## 修改前必须先做的事

在开始修改前：

1. 先阅读与本任务直接相关的文件，不要盲改。
2. 判断是否已有相似实现、已有工具函数、已有配置项。
3. 优先复用现有模式，保持与仓库现有风格一致。
4. 如果发现需求会牵连大范围重构，应改为提出最小可行补丁，而不是直接扩散修改范围。

---

## 修改时必须遵守的实现要求

### 1) 保持兼容性

- 除非任务明确要求，保持现有命令行为、配置默认值、返回文案风格和数据读写格式兼容。
- 涉及持久化 JSON / 索引 / 缓存 / 日志结构时，优先向后兼容。

### 2) 注释与文案

- 仅在必要时新增或修改注释。
- 中文文案必须保持自然、简洁，避免出现乱码或半角/全角混乱。
- **不要**为了“润色”而批量改写现有中文提示。

### 3) 日志

- 不要额外扩大日志范围。
- 不要把敏感路径、用户内容、隐私信息无必要地写入日志。
- 日志变更应保持克制。

### 4) 依赖

- 非必要不要新增第三方依赖。
- 如必须新增依赖，必须同时更新对应依赖声明，并说明用途。
- 对可选依赖，优先采用“缺失时可降级”的方式，而不是让整个项目直接崩溃。

---

## 测试与验证要求

完成修改后，按任务相关性尽量执行以下检查：

1. 运行与改动直接相关的测试
2. 若存在快速静态检查或最小运行验证，应一并执行
3. 确认没有无关文件被大面积改动
4. 检查是否引入编码变化、乱码、异常换行变更

如果当前环境缺依赖或无法完整运行，需要明确说明：

- 哪一步无法执行
- 原因是什么
- 已完成了哪些替代性检查

**不要**假装测试通过。

---

## 输出要求

提交结果时必须：

1. 先简述修改了什么
2. 列出修改的文件
3. 说明为什么这样改
4. 说明验证了什么
5. 明确指出是否存在风险或未覆盖边界

如果任务要求输出 diff 或补丁：

- 优先输出**精简 diff**
- 不要附带大段无关格式化改动

---

## 明确禁止的行为

- 禁止整文件重写以完成小需求
- 禁止批量格式化整个仓库
- 禁止批量修改中文文案
- 禁止随意统一所有文件换行符
- 禁止引入与任务无关的“顺手优化”
- 禁止在未说明的情况下修改配置默认值
- 禁止在未验证的情况下声称“已完全修复”
- 禁止删除现有保护性逻辑，只因为测试或实现更省事

---

## 本项目的优先策略

针对本仓库，默认优先级如下：

1. **稳定性优先于整洁性**
2. **兼容性优先于理想化重构**
3. **最小补丁优先于大改架构**
4. **保护中文编码与换行稳定性优先于格式统一**

如果需求与这些原则冲突，优先选择：

- 范围更小的实现
- 更不容易引发回归的实现
- 更不容易导致中文乱码、整文件 diff、测试失真的实现

---

## 建议工作方式

推荐按以下顺序工作：

1. 阅读相关文件
2. 给出最小改动方案
3. 只修改必要位置
4. 运行最小验证
5. 检查 diff 是否干净
6. 再输出结果

若发现 diff 超出预期，应优先回退并改用更小补丁。
